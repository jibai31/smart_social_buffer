| var previewBtn = $('#jsPreviewWeek#{@week.id}');
| var planBtn = $('#jsPlanWeek#{@week.id}');
| previewBtn.hide();
| previewBtn.parents('.step').addClass('done');
| planBtn.css("display", "block");
| planBtn.parents('.step').removeClass('next');

| var previewWeek = $("#{escape_javascript(render @week)}");
| var week = $('#week#{@week.id}');
| week.replaceWith(previewWeek);

h1 Welcome #{current_user.name}!

p You have successfully linked the following accounts:
.social
  .social-signin-buttons
    = render 'authentications/authentication', provider: "facebook"
    = render 'authentications/authentication', provider: "google_oauth2"
    = render 'authentications/authentication', provider: "twitter"
    = render 'authentications/authentication', provider: "linkedin"
